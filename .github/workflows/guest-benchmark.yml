# ZisK guest correctness, execution-step evidence, and strict release qualification.
name: Guest benchmark

on:
  pull_request:
    paths:
      - build.zig
      - build.zig.zon
      - src/**
      - guest/**
      - eest/**
      - .github/actions/build-zisk-artifacts/**
      - .github/workflows/guest-benchmark.yml
  push:
    branches: [main]
    paths:
      - build.zig
      - build.zig.zon
      - src/**
      - guest/**
      - eest/**
      - .github/actions/build-zisk-artifacts/**
      - .github/workflows/guest-benchmark.yml
  schedule:
    - cron: "23 2 * * *"
  workflow_dispatch:
    inputs:
      corpus:
        description: Corpus selection
        required: true
        default: pinned
        type: choice
        options:
          - pinned
          - rolling
          - tests-zkevm
      release_gate:
        description: Strict release qualification; tests-zkevm only
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: guest-benchmark-${{ github.event.pull_request.number || github.ref_name }}-${{ github.event_name == 'schedule' && 'rolling' || github.event_name == 'workflow_dispatch' && inputs.corpus || 'tests-zkevm' }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  measure:
    name: zisk ${{ github.event_name == 'schedule' && 'rolling' || github.event_name == 'workflow_dispatch' && inputs.corpus || 'tests-zkevm' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    env:
      CORPUS_MODE: ${{ github.event_name == 'schedule' && 'rolling' || github.event_name == 'workflow_dispatch' && inputs.corpus || 'tests-zkevm' }}
      CATALOG_URL: https://pub-df22334654034ebab51bc096137a59d8.r2.dev/devnets/glamsterdam-devnet-7
      PINNED_MANIFEST: eest/fixtures/devnet-glamsterdam-7-pinned.json
      CORPUS_ROOT: ${{ github.workspace }}/devnet-corpus
      RESOLVED_MANIFEST: ${{ github.workspace }}/resolved-manifest.json
      SOURCE_COMMIT: ${{ github.sha }}
      STATELESS_SCHEMA: "0x1501"
      ZIG_VERSION: 0.16.0
      ZISK_VERSION: 1.0.0-alpha
      ZISK_COMMIT: 4b9f758fabc4955cac20af837019ccc31b803a46
      ZISK_RUST_TOOLCHAIN: zisk-1.0.0

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: ${{ inputs.release_gate && 0 || 1 }}

      - name: Require release qualification inputs
        if: inputs.release_gate
        run: |
          test "${CORPUS_MODE}" = tests-zkevm
          git fetch --no-tags origin main
          git merge-base --is-ancestor "${SOURCE_COMMIT}" FETCH_HEAD

      - name: Resolve runner paths
        run: |
          {
            echo "ZISK_HOME=${RUNNER_TEMP}/zisk-sdk"
            echo "ZISK_SOURCE=${RUNNER_TEMP}/zisk-source"
            echo "ZISK_ARTIFACTS=${RUNNER_TEMP}/zisk-artifacts"
            echo "ZISK_CACHE_KEY=zisk-artifacts-v1-${RUNNER_OS}-${RUNNER_ARCH}-ubuntu-24.04-${ZISK_VERSION}-${ZISK_COMMIT}-${ZISK_RUST_TOOLCHAIN}-${{ hashFiles('.github/actions/build-zisk-artifacts/action.yml', 'guest/runtime/zisk/host/Cargo.toml', 'guest/runtime/zisk/host/Cargo.lock', 'guest/runtime/zisk/host/src/**') }}"
          } >> "${GITHUB_ENV}"

      - name: Restore pinned ZisK artifacts
        id: zisk-cache
        uses: actions/cache/restore@v6
        with:
          path: ${{ runner.temp }}/zisk-artifacts
          key: ${{ env.ZISK_CACHE_KEY }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            jq \
            libgmp10 \
            libomp5-18 \
            libopenmpi3t64 \
            libsodium23 \
            zstd

      - name: Install Zig
        uses: mlugg/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}
          use-cache: ${{ github.event_name != 'pull_request' }}
          cache-key: guest-benchmark-${{ github.event_name }}-${{ env.CORPUS_MODE }}

      - name: Resolve pinned corpus
        if: env.CORPUS_MODE == 'pinned'
        run: |
          python3 eest/scripts/fetch-devnet-batches.py \
            --manifest "${PINNED_MANIFEST}" \
            --network glamsterdam-devnet-7 \
            --output-root "${CORPUS_ROOT}" \
            --resolved-manifest "${RESOLVED_MANIFEST}"

      - name: Resolve rolling corpus
        if: env.CORPUS_MODE == 'rolling'
        run: |
          python3 eest/scripts/fetch-devnet-batches.py \
            --catalog-url "${CATALOG_URL}" \
            --latest-batches 10 \
            --network glamsterdam-devnet-7 \
            --output-root "${CORPUS_ROOT}" \
            --resolved-manifest "${RESOLVED_MANIFEST}"

      - name: Fetch tests-zkevm corpus
        if: env.CORPUS_MODE == 'tests-zkevm'
        working-directory: eest
        env:
          EEST_CACHE: ${{ runner.temp }}/eest-download
        run: |
          trap 'rm -rf -- "${EEST_CACHE}"' EXIT
          scripts/fetch-eest-zkevm-fixtures.sh \
            --resolved-manifest "${RESOLVED_MANIFEST}"

      - name: Build pinned ZisK artifacts
        if: steps.zisk-cache.outputs.cache-hit != 'true'
        uses: ./.github/actions/build-zisk-artifacts
        with:
          version: ${{ env.ZISK_VERSION }}
          commit: ${{ env.ZISK_COMMIT }}
          rust-toolchain: ${{ env.ZISK_RUST_TOOLCHAIN }}
          zisk-home: ${{ env.ZISK_HOME }}
          source-dir: ${{ env.ZISK_SOURCE }}
          artifacts-dir: ${{ env.ZISK_ARTIFACTS }}
          build-host: "true"
          host-manifest: ${{ github.workspace }}/guest/runtime/zisk/host/Cargo.toml

      - name: Resolve pinned ZisK artifacts
        id: zisk
        run: |
          host="${ZISK_ARTIFACTS}/evmz-zisk-host"
          provider="${ZISK_ARTIFACTS}/libziskos_staticlib.a"
          test -x "${host}"
          test -s "${provider}"
          {
            echo "host=${host}"
            echo "provider=${provider}"
          } >> "${GITHUB_OUTPUT}"

      - name: Build and validate guest
        env:
          PROVIDER: ${{ steps.zisk.outputs.provider }}
          RELEASE_GATE: ${{ inputs.release_gate }}
        run: |
          zig build guest-zisk \
            -Dguest-payload=stateless-ere \
            "-Dstateless-schema=${STATELESS_SCHEMA}" \
            -Doptimize=ReleaseFast \
            "-Dziskos-staticlib=${PROVIDER}" \
            --prefix "${RUNNER_TEMP}/guest" \
            --global-cache-dir "${RUNNER_TEMP}/zig-global-cache" \
            --summary all
          elf="${RUNNER_TEMP}/guest/guest/zisk/evmz-guest-zisk.elf"
          candidate="${GITHUB_WORKSPACE}/stateless-validator-evmz-zisk-${ZISK_VERSION}.elf"
          install -D -m 0644 "${elf}" "${candidate}"
          zig build check-guest-elf -- \
            --require-rv64im-zicclsm \
            --allow-attribute-a \
            "${candidate}"
          if [[ "${RELEASE_GATE}" == true ]]; then
            zig build guest-zisk \
              -Dguest-payload=exit-failure-probe \
              -Doptimize=ReleaseFast \
              "-Dziskos-staticlib=${PROVIDER}" \
              --prefix "${RUNNER_TEMP}/failure-probe" \
              --global-cache-dir "${RUNNER_TEMP}/zig-global-cache" \
              --summary all
            zig build check-zisk-failure-status -- --expect-no-propagation \
              "${{ steps.zisk.outputs.host }}" \
              "${RUNNER_TEMP}/failure-probe/guest/zisk/evmz-guest-zisk.elf"
          fi
          echo "CANDIDATE_ELF=${candidate}" >> "${GITHUB_ENV}"

      - name: Run corpus and produce evidence
        env:
          GUEST_HOST: ${{ steps.zisk.outputs.host }}
          RELEASE_GATE: ${{ inputs.release_gate }}
        run: |
          args=(
            --executor zisk
            --jobs "$(nproc)"
            --zisk-host "${GUEST_HOST}"
            --zisk-elf "${CANDIDATE_ELF}"
            --evidence-dir "${GITHUB_WORKSPACE}/guest-evidence"
            --corpus-manifest "${RESOLVED_MANIFEST}"
            --known-failures "${GITHUB_WORKSPACE}/eest/fixtures/guest-known-failures.json"
            --source-ref "${SOURCE_COMMIT}"
            --stateless-schema "${STATELESS_SCHEMA}"
            --zig-version "${ZIG_VERSION}"
            --backend-version "${ZISK_VERSION}"
            --backend-commit "${ZISK_COMMIT}"
            --backend-toolchain "${ZISK_RUST_TOOLCHAIN}"
          )
          if [[ "${RELEASE_GATE}" == true ]]; then
            args+=(--strict-evidence)
          fi
          zig build -Doptimize=ReleaseFast zkevm --summary all -- "${args[@]}"

      - name: Publish job summary
        if: always() && hashFiles('guest-evidence/report.md') != ''
        run: cat guest-evidence/report.md >> "${GITHUB_STEP_SUMMARY}"

      - name: Upload diagnostic evidence
        if: always() && !inputs.release_gate
        uses: actions/upload-artifact@v7
        with:
          name: guest-zisk-${{ env.CORPUS_MODE }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: guest-evidence
          if-no-files-found: warn
          retention-days: 14

      - name: Upload strict release evidence
        if: success() && inputs.release_gate
        uses: actions/upload-artifact@v7
        with:
          name: guest-zisk-strict-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            ${{ env.CANDIDATE_ELF }}
            guest-evidence/evidence.json
            guest-evidence/report.md
          if-no-files-found: error
          retention-days: 90

      - name: Save pinned ZisK artifacts from main
        if: >-
          always()
          && (github.event_name == 'schedule' || (github.event_name == 'push' && github.ref == 'refs/heads/main'))
          && steps.zisk-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        with:
          path: ${{ runner.temp }}/zisk-artifacts
          key: ${{ env.ZISK_CACHE_KEY }}

{
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "sh -c 'if ! command -v kraft >/dev/null 2>&1; then exit 0; fi; exec kraft hooks codex pre-tool-use'",
            "type": "command"
          }
        ]
      }
    ]
  }
}